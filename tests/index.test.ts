import { expect } from "chai";
import "mocha";

describe("Fake Test", () => {
  it("Will Pass", () => {
    expect(2).to.equal(2);
  });
  it("Will Fail", () => {
    expect(2).to.equal(2);
  });
});
